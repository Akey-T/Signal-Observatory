# E04.5 Data Health & Coverage Ledger Acceptance

Evidence date: 2026-08-11 UTC

Result: **Accepted — E04.5 implementation and verification complete**

This result applies only to E04.5. It does **not** close the E04 second-UTC-day gate or the E03
seven-day scheduler soak. Both time-dependent gates remain truthfully `PENDING` and no observation
was generated, edited, interpolated, or relabeled to satisfy them.

## Delivered capability

E04.5 adds a deterministic, rebuildable operational truth layer:

```text
Persisted observations, cursors and runs
  -> Topic × Source coverage projection
  -> collector health, freshness and data-quality aggregation
  -> read-only Operations/Coverage APIs and verification CLI
  -> /operations and compact Topic coverage summaries
```

Migration `20260811_0005_data_health_coverage.py` creates
`topic_source_coverage`. The projection stores target and observed windows, actual/missing counts,
partial reasons, source strategy and `derivation_version = coverage-v1`. It does not modify Raw
evidence or collector observation semantics.

## Coverage evidence

The production-like local database was rebuilt twice. The final repeat processed 96 projections
with `created = 0`, `updated = 0`, `unchanged = 96`, proving idempotency against unchanged facts.
Unchanged rows retained their prior `derived_at` value.

| Source    | Complete | Partial | Forward only | Empty | Unknown |
| --------- | -------: | ------: | -----------: | ----: | ------: |
| arXiv     |        2 |       3 |            0 |     0 |      37 |
| GitHub    |        0 |       0 |            6 |     0 |      48 |
| **Total** |    **2** |   **3** |        **6** | **0** |  **85** |

The real arXiv `complete` Topics are Data Lakehouse and Site Reliability Engineering. The real
`partial` Topics are Artificial Intelligence, Model Context Protocol and Retrieval-Augmented
Generation; each exposes its unresolved historical cursor reason. A successful incremental run
without a declared historical target remains `unknown`, never silently complete.

GitHub has one real UTC observation date, `2026-08-11`, and six `forward_only` Topic projections:
Grafana, Model Context Protocol, OpenTofu, PostgreSQL, Prometheus and Terraform. Their coverage
starts and ends on the real observed date. Missing dates between the current first/latest dates are
zero; no pre-observation history is implied.

Automated derivation tests cover arXiv complete, partial and completed zero-result (`empty`),
GitHub one-day/two-day forward-only, a missing intermediate day, never initialized (`unknown`),
completed empty discovery, unimplemented source channels, derivation version and rebuild
idempotency.

## Operations evidence

The live aggregate returned:

| Area                    | State          | Persisted evidence                                            |
| ----------------------- | -------------- | ------------------------------------------------------------- |
| Registry                | Healthy        | version 1, 0 warnings                                         |
| Research / arXiv        | Degraded       | latest run partial, 42 mappings, 3 partial mappings, 2 errors |
| Developer / GitHub      | Healthy        | auth configured, 51 tracked repositories, 1 snapshot date     |
| Community / Hacker News | Not collecting | collector not implemented; no fake coverage row               |
| Public / Wikipedia      | Not collecting | collector not implemented; no fake coverage row               |
| Overall Observatory     | Degraded       | Research is degraded and three projections are partial        |

Freshness is threshold-driven and separate from health and coverage. The active Research and
Developer evidence was fresh at acceptance time. Data Quality reported two ingestion errors in the
last 24 hours, three partial mappings, two stale/unresolved cursors, zero missing GitHub dates and
zero Registry warnings. Raw checksum failures remain `not_verified` in the page/API until a
bounded verifier runs; the UI does not fabricate a zero.

`GET /api/operations`, `GET /api/coverage` with Source/Status/Topic pagination filters, and
`GET /api/topics/{slug}/coverage` passed integration and live HTTP checks. Community/Public return
`collection_state = not_started` without inserting fake projections.

## Verification commands

| Check                                | Result               | Evidence                                                                   |
| ------------------------------------ | -------------------- | -------------------------------------------------------------------------- |
| `coverage rebuild --json` repeat     | Passed               | 96 unchanged, 0 created/updated/deleted                                    |
| `ops check --json`                   | Degraded as designed | Raw sample passed; migration current at `20260811_0005`; exit 1            |
| `ops verify-raw --sample 100 --json` | Passed               | 100/100 checked from 130 available, 0 failures, 0 modified                 |
| `github verify-cross-day --json`     | **Pending**          | only `2026-08-11`; exit 1; 0 modified                                      |
| `arxiv verify-soak --days 7 --json`  | **Pending**          | 0 scheduler-tagged runs in the new verification window; exit 1; 0 modified |

The Raw verifier checked database pointers, filesystem payloads, sidecars and checksums. Automated
cross-day tests cover pending, changed, unchanged, missing-date, Raw mutation, incorrect aggregate
delta and poll-state inconsistency. Soak tests cover pending, seven-day success with legitimate
partial runs, missing windows and duplicate scheduling.

## UI and browser acceptance

`/operations` presents the required title and subtitle, explained overall state, four Source cards,
Data Quality and a Topic × Source Coverage Matrix. Search, Source and Coverage-state filters passed
interactive testing. The page displays status/date facts and no completeness or health percentage.

Without reading logs, the browser answered:

- Research is Degraded because its latest run and three historical mappings are partial.
- GitHub has one real snapshot date and begins on 2026-08-11.
- Artificial Intelligence, Model Context Protocol and Retrieval-Augmented Generation have partial
  Research history.
- Hacker News and Wikipedia are not collecting.

Topic Detail for Model Context Protocol displayed Research `partial`, Developer `forward_only`,
Community/Public `not started`, and the Operations link. Browser console warnings/errors: zero.

## Quality gates

| Gate                                                | Result                                                                 |
| --------------------------------------------------- | ---------------------------------------------------------------------- |
| `python -m ruff check .`                            | Passed                                                                 |
| `python -m ruff format --check .`                   | Passed; 149 files formatted                                            |
| `python -m mypy apps packages config db collectors` | Passed; 71 source files                                                |
| `python -m pytest`                                  | Passed; 155 passed, 1 skipped                                          |
| `npm run lint`                                      | Passed                                                                 |
| `npm run format:check`                              | Passed                                                                 |
| `npm run typecheck`                                 | Passed                                                                 |
| `npm run test`                                      | Passed; 33 tests                                                       |
| `npm run build`                                     | Passed; 53 modules transformed                                         |
| `docker compose config`                             | Passed                                                                 |
| Docker Compose build/health                         | Passed; db, api, worker and web healthy                                |
| Empty PostgreSQL migration                          | Passed at `20260811_0005`; 27 public tables                            |
| Browser smoke                                       | Passed; `/operations`, filters and Topic coverage; zero console issues |
| Core TODO/stub audit                                | Passed; no E04.5 core TODO or stub                                     |

An independent empty database named `signal_observatory_e045_acceptance` ran the complete Alembic
chain from E00 through E04.5, contained the coverage table, and was deleted after inspection.

## Known limitations and deliberately deferred work

- E04 remains not accepted until a second real UTC-day GitHub Snapshot exists and
  `github verify-cross-day` passes.
- E03 remains in its real seven-day operational soak; manual runs do not count as scheduler days.
- Community/Hacker News and Public/Wikipedia collectors are not implemented.
- No Trend Engine, Trend Score, momentum, acceleration, forecast, cross-source correlation, AI
  diagnosis, alerts, Prometheus/Grafana stack, Airflow, Kafka or Kubernetes was added.
- Raw integrity results are intentionally not persisted yet; API/page state remains not verified
  between explicit CLI checks rather than displaying a false zero.
- Backup/restore scripts and manifest verification are deferred to a separately scoped E04.5B. A
  future implementation must back up and restore both PostgreSQL and immutable Raw storage.

## Next gate and next Epic

Continue accumulating real E03 scheduled evidence and wait for the second real GitHub UTC day. Run
the two read-only verification commands after those windows exist. Only after the E04 acceptance
document becomes **Accepted** should planning proceed to **E05 — Hacker News Community Collector**.
