"""Gold-layer interfaces; metric implementations are intentionally out of scope."""

from observatory_analytics.interfaces import GoldMetric, GoldMetricReader

__all__ = ["GoldMetric", "GoldMetricReader"]
